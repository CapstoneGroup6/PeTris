// require('dotenv').config()
import './App.css';

console.log(process.env.FIREBASE_API_KEY);
function App() {
  return (
    <div className="App">
      <header className="App-header">
      </header>
    </div>
  );
}

export default App;
